---
// src/components/projects/ProjectCard.astro
// Individual project card matching reference design:
// Icon + title/badges | description | divider | Live Demo + GitHub

import ReSubsIcon from './icons/ReSubsIcon.astro';
import MomentalIcon from './icons/MomentalIcon.astro';
import AnalyticsIcon from './icons/AnalyticsIcon.astro';
import type { Project } from '../../data/projects';
import { techBadgeColors } from '../../data/projects';

interface Props {
  project: Project;
}

const { project } = Astro.props;

// Derive fallback if explicit fallbackText is omitted
const derivedFallback = project.fallbackText || project.title.substring(0, 2).toUpperCase();
const containerBg = project.brandColor || 'var(--bg-main)';
---

<article class="project-card" data-category={project.category}>
  <div class="card-header">
    <div class="project-icon" style={`background-color: ${containerBg}; color: ${project.id === 'streamguide' ? '#000000' : '#ffffff'}; font-weight: ${project.id === 'streamguide' ? '900' : '700'}`}>
      {project.iconImage ? (
        <img src={project.iconImage} alt={`${project.title} logo`} class="project-img-icon" />
      ) : project.icon === 'resubs' ? (
        <ReSubsIcon />
      ) : project.icon === 'momental' ? (
        <MomentalIcon />
      ) : project.icon === 'analytics' ? (
        <AnalyticsIcon />
      ) : (
        <span>{derivedFallback}</span>
      )}
    </div>
    <div class="card-info">
      <h3 class="project-name">
        <a href={`/projects/${project.id}`} class="project-link">{project.title}</a>
      </h3>
      <div class="tech-badges">
        {project.techStack.map((tech) => (
          <span
            class="badge"
            style={`background-color: ${techBadgeColors[tech] || '#6B7280'}`}
          >
            {tech}
          </span>
        ))}
      </div>
    </div>
  </div>

  <p class="project-desc">{project.description}</p>

  <hr class="card-divider" />

  <div class="card-footer" style={!project.demoUrl && project.githubUrl ? 'justify-content: flex-end;' : ''}>
    {project.demoUrl && (
      <a href={project.demoUrl} class="demo-link" target="_blank" rel="noopener noreferrer">
        Live Demo
        <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"></path>
          <polyline points="15 3 21 3 21 9"></polyline>
          <line x1="10" y1="14" x2="21" y2="3"></line>
        </svg>
      </a>
    )}
    {project.githubUrl && (
      <a href={project.githubUrl} class="github-link" target="_blank" rel="noopener noreferrer" aria-label={`${project.title} on GitHub`}>
        <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="currentColor">
          <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
        </svg>
      </a>
    )}
  </div>
</article>

<style>
  .project-card {
    background: var(--bg-surface);
    border: 1px solid var(--border-subtle);
    border-radius: 12px;
    padding: 1.25rem;
    display: flex;
    flex-direction: column;
    transition: box-shadow 0.2s ease;
  }

  .project-card:hover {
    box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06);
  }

  .card-header {
    display: flex;
    align-items: flex-start;
    gap: 0.875rem;
    margin-bottom: 0.75rem;
  }

  .project-icon {
    width: 44px;
    height: 44px;
    min-width: 44px;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #ffffff;
    font-weight: 700;
    font-size: 0.95rem;
    letter-spacing: 0.02em;
  }

  .card-info {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    min-width: 0;
  }

  .project-name {
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--text-primary);
    line-height: 1.3;
  }

  .project-link {
    color: inherit;
    text-decoration: none;
    transition: opacity 0.2s ease;
  }

  .project-link:hover {
    opacity: 0.8;
  }

  .tech-badges {
    display: flex;
    flex-wrap: wrap;
    gap: 0.3rem;
  }

  .badge {
    font-size: 0.625rem;
    font-weight: 600;
    color: #ffffff;
    padding: 0.125rem 0.45rem;
    border-radius: 4px;
    letter-spacing: 0.02em;
    white-space: nowrap;
    line-height: 1.5;
  }

  .project-desc {
    font-size: 0.85rem;
    color: var(--text-secondary);
    line-height: 1.55;
    flex: 1;
  }

  .card-divider {
    border: none;
    border-top: 1px solid var(--border-subtle);
    margin: 0.875rem 0;
  }

  .card-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .demo-link {
    display: flex;
    align-items: center;
    gap: 0.35rem;
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--accent-blue);
    text-decoration: none;
    transition: opacity 0.2s ease;
  }

  .demo-link:hover {
    opacity: 0.8;
  }

  .github-link {
    color: var(--text-primary);
    display: flex;
    align-items: center;
    transition: opacity 0.2s ease;
  }

  .github-link:hover {
    opacity: 0.7;
  }

  :global(html.dark) .project-card:hover {
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
  }
</style>

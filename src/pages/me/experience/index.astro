---
import MeLayout from '../../../layouts/MeLayout.astro';
---

<MeLayout title="Experience | Portfolio Management" activeNav="experience">
  <div class="cms-container">
    <header class="cms-header">
      <div>
        <h1 class="page-title">Experience</h1>
        <p class="page-subtitle">Manage your work experience</p>
      </div>
      <div class="header-actions">
        <a href="/me/experience/edit" class="primary-btn">Add Experience</a>
        <a href="/me" class="secondary-btn">Dashboard</a>
      </div>
    </header>

    <hr class="section-divider" />

    <div class="list-container" id="experience-list">
      <p class="loading-text">Loading experience...</p>
    </div>
  </div>
</MeLayout>

<style>
  .cms-container {
    max-width: 900px;
    margin: 0 auto;
    padding: 3rem 1.5rem;
  }

  .cms-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
  }

  .page-title {
    font-size: 2rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0 0 0.35rem 0;
  }

  .page-subtitle {
    font-size: 0.95rem;
    color: var(--text-secondary);
    margin: 0;
  }

  .header-actions {
    display: flex;
    gap: 1rem;
  }

  .primary-btn {
    background-color: var(--text-primary);
    color: var(--bg-main);
    padding: 0.6rem 1.25rem;
    border-radius: 8px;
    font-size: 0.9rem;
    font-weight: 600;
    text-decoration: none;
    transition: opacity 0.2s ease;
  }

  .primary-btn:hover {
    opacity: 0.9;
  }

  .secondary-btn {
    background-color: transparent;
    border: 1px solid var(--border-subtle);
    color: var(--text-secondary);
    padding: 0.6rem 1.25rem;
    border-radius: 8px;
    font-size: 0.9rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .secondary-btn:hover {
    background-color: var(--bg-surface);
    color: var(--text-primary);
    border-color: var(--text-secondary);
  }

  .section-divider {
    border: none;
    border-top: 1px solid var(--border-subtle);
    margin: 1.5rem 0 2rem 0;
  }

  .loading-text, .empty-text {
    color: var(--text-secondary);
    font-size: 0.95rem;
  }

  .list-container {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  :global(.list-item) {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background-color: var(--bg-surface);
    border: 1px solid var(--border-subtle);
    padding: 1.25rem 1.5rem;
    border-radius: 12px;
  }

  :global(.item-info) {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    flex: 1;
  }

  :global(.item-title) {
    font-size: 1.1rem;
    font-weight: 600;
    color: var(--text-primary);
    margin: 0;
  }

  :global(.item-meta) {
    font-size: 0.85rem;
    color: var(--text-secondary);
  }

  :global(.item-status) {
    width: 120px;
  }

  :global(.status-badge) {
    font-size: 0.75rem;
    font-weight: 600;
    padding: 0.25rem 0.6rem;
    border-radius: 6px;
    border: 1px solid var(--border-subtle);
  }

  :global(.status-badge.published) {
    background-color: rgba(16, 185, 129, 0.1);
    color: #10b981;
    border-color: rgba(16, 185, 129, 0.2);
  }

  :global(.status-badge.draft) {
    background-color: var(--bg-main);
    color: var(--text-secondary);
  }

  :global(.item-actions) {
    display: flex;
    gap: 0.75rem;
  }

  :global(.action-btn) {
    background: transparent;
    border: 1px solid var(--border-subtle);
    color: var(--text-secondary);
    padding: 0.4rem 0.8rem;
    border-radius: 6px;
    font-size: 0.85rem;
    font-weight: 500;
    cursor: pointer;
    text-decoration: none;
    transition: all 0.2s ease;
  }

  :global(.action-btn:hover) {
    color: var(--text-primary);
    border-color: var(--text-secondary);
  }

  :global(.delete-btn:hover) {
    color: #ef4444;
    border-color: #ef4444;
  }
</style>

<script>
import { db } from '../../../lib/firebase';
import { ref, onValue, remove } from 'firebase/database';

  document.addEventListener('astro:page-load', () => {
  
  

  if ((window as any).__expUnsubscribe) {
    (window as any).__expUnsubscribe();
    (window as any).__expUnsubscribe = null;
  }

  const listEl = document.getElementById('experience-list');
  if (!listEl) return;

  const expRef = ref(db, 'experience');

  (window as any).__expUnsubscribe = onValue(expRef, (snapshot) => {
    
    if (!snapshot.exists()) {
      listEl.innerHTML = '<p class="empty-text">No experience entries found.</p>';
      return;
    }

    const entries: any[] = [];
    snapshot.forEach((child) => {
      entries.push({ dbKey: child.key, ...child.val() });
    });

    // Sort by order ascending
    entries.sort((a, b) => (a.order || 0) - (b.order || 0));

    let html = '';
    entries.forEach(e => {
      const dates = e.startDate + (e.endDate ? ` - ${e.endDate}` : ' - Present');
      html += `
        <div class="list-item">
          <div class="item-info">
            <h3 class="item-title">${e.role || 'Untitled Role'}</h3>
            <span class="item-meta">${e.company || 'Unknown Company'} • ${dates} • Order: ${e.order || 0}</span>
          </div>
          <div class="item-status">
            <span class="status-badge ${e.published ? 'published' : 'draft'}">${e.published ? 'Published' : 'Draft'}</span>
          </div>
          <div class="item-actions">
            <a href="/me/experience/edit?id=${e.dbKey}" class="action-btn">Edit</a>
            <button class="action-btn delete-btn" data-id="${e.dbKey}">Delete</button>
          </div>
        </div>
      `;
    });

    listEl.innerHTML = html;

    // Attach delete listeners
    document.querySelectorAll('.delete-btn').forEach(btn => {
      btn.addEventListener('click', async (evt) => {
        const id = (evt.target as HTMLButtonElement).dataset.id;
        if (id && confirm('Are you sure you want to delete this experience entry? This cannot be undone.')) {
          try {
            await remove(ref(db, `experience/${id}`));
          } catch (err) {
            console.error('Failed to delete entry:', err);
            alert('Failed to delete entry.');
          }
        }
      });
    });
  });
  });
</script>
